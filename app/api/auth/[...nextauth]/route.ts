/**
 * NextAuth.js API Route Handler
 *
 * Handles all authentication routes including sign in, sign out, and callbacks.
 * Requirements: 3.1, 3.2, 6.1, 6.2
 */

export { GET, POST } from '@/lib/auth/auth';
